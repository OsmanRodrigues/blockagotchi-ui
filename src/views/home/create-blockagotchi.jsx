import { SectionWrapper } from '../../atomic/wrapper/section.wrapper';

export const CreateBlockagotchiSection = () => {
    return (
        <SectionWrapper>
            <p>Create section</p>
        </SectionWrapper>
    );
};
